pipeline {
    agent any

    stages {
        stage('code pull') {
            steps {
                git 'https://github.com/akale9888/student-file.git'
            }
        }
        stage('build') {
            steps {
                sh'mvn clean package'
            }
        }
    }
}
