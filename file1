pipeline {
    agent any

    stages {
        stage('code pushed') {
            steps {
                sh'ls'
            }
        }
        stage('build') {
            steps {
                sh'mvn clean package'
            }
        }
    }
}
